What the signature says         | So what Breaks      | So you build
A function, no self - nothing.    If forgets everything Memory/ RAG
persist between calls 

One argument, not f(instructions, "instructions vs data". Security
data)                              is unenforcable - there's
                                   no second slot to put in
                                    trust
Returns a sample, not a             Same input, different Evals
computation. call it twice,         output
get two answers.     


The signature says.             | So what breaks    | So youu  build
A function, no self-nothing       Forgets everything Memory/RAG
persists between calls

One argument, not f(instructions, "instructions v data" Security
data)                               is unenforcable - 
                                    there's no second
                                    slot to put trust

Returns a sample, not a            Same input, different   Evals 
computation                            output

The signature | What breaks | You Build
a function, no forgets        Memory/ RAG
self - nothing everything
persist between 
calls

One argument,   instructions v data     Security
not f(instructions, is unenforcable
data)           and no second slot
                to put trust in

A sample is returned same input     Evals
not a compulation   different input
called twice. 
